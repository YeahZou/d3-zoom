Zoom Transforms
  缩放信息被存储在应用了缩放行为的元素上，而不是存储在缩放行为里，因为同一个缩放行为可以被应用在多个元素上，每个元素可以独立进行缩放操作。
缩放状态会随用户的交互而改变，也可以使用 zoom.transform方法通过程序的方式改变。
  为了获取缩放状态，可以在缩放事件的回调函数中通过event.transform属性得到，也可以使用d3.zoomTransform方法,后者在想通过程序修改缩放状态时特别有用。
